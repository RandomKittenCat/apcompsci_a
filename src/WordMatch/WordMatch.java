package WordMatch;

public class WordMatch {
    private String secret;

    public WordMatch(String word){
        /* implementation not shown */
    }

    public int scoreGuess(String guess){
        int occurences = 0;
        int lastIndex = 0;

        while(lastIndex <= secret.length()){

        }
    }

}
